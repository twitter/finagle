target(name='benchmark',
  dependencies=[
    'finagle/finagle-exp/src/main/scala:benchmark'
  ]
)

target(name='finagle-exp',
  dependencies=[
    'finagle/finagle-exp/src/main/scala'
  ]
)

target(name='tests',
  dependencies=[
    'finagle/finagle-exp/src/test/scala'
  ]
)
